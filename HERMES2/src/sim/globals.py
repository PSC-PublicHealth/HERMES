#!/usr/bin/env python


_hermes_svn_id_="$Id$"

__doc__=""" globals.py
This is a place to store data structures which are used by all runs but
which are not constants per se, and so don't belong in the constants module.
"""

defaultInputTokenFile = "input_default.csv"

deterministic = False
